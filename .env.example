# Copy to `.env` (git-ignored) for local runs, or just `export` these in your shell.
# The app reads ANTHROPIC_API_KEY to use real Claude; without it, the offline mock runs.

ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default engagement bundle path
# PBC_BUNDLE=data/sample_bundle
